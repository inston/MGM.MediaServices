version: '3.8'

services:
  mediaservices:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./media:/app/media
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
